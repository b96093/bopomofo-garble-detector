# 核心引擎 Implementation Plan（計畫 1／3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 做出純 JS 的轉換引擎，把「忘了切輸入法的大千注音亂碼」轉成最可能的漢字串（例：`ji394t au04` → `我愛吃麵`）。

**Architecture:** 兩個純函式模組。`layout.js` 用大千對照表把英文鍵位還原成注音音節（聲調鍵當分隔點，100% 確定性）。`convert.js` 以「最長詞優先」在詞庫中選字。詞庫以參數注入（依賴注入），本計畫用小型 fixture 詞庫測試，真實詞庫留待計畫 2。全程 `node --test`、零第三方相依。

**Tech Stack:** Node.js 內建測試（`node --test`）、ES Modules（`"type":"module"`）、無任何 npm 相依套件。

---

## File Structure

- `package.json` — 專案設定，`type: module`，`test` script。
- `.gitignore` — 忽略 `node_modules`。
- `src/engine/layout.js` — 大千對照表 + `keysToZhuyin(input)`。純函式，無相依。
- `src/engine/convert.js` — `convert(input, dict)`。相依 `layout.js`。詞庫由參數注入。
- `test/layout.test.js` — `keysToZhuyin` 單元測試。
- `test/convert.test.js` — `convert` 單元測試 + golden 範例（含 fixture 詞庫）。

每個 task 產出自足、可獨立驗證的變更，結束即 commit。

---

### Task 1: 專案骨架

**Files:**
- Create: `package.json`
- Create: `.gitignore`
- Create: `test/smoke.test.js`

- [ ] **Step 1: 寫 package.json**

Create `package.json`:

```json
{
  "name": "text-garble-detector",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test"
  }
}
```

- [ ] **Step 2: 寫 .gitignore**

Create `.gitignore`:

```
node_modules/
```

- [ ] **Step 3: 寫一個 smoke 測試確認測試框架能跑**

Create `test/smoke.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';

test('測試框架可運作', () => {
  assert.equal(1 + 1, 2);
});
```

- [ ] **Step 4: 執行測試確認通過**

Run: `npm test`
Expected: 1 test passed（`tests 1 / pass 1`）。

- [ ] **Step 5: Commit**

```bash
git add package.json .gitignore test/smoke.test.js
git commit -m "chore: 專案骨架與 node --test 測試框架"
```

---

### Task 2: 鍵位 → 注音（layout.js）

實作大千對照表與 `keysToZhuyin`。關鍵規則：聲調鍵（`空白`=一聲、`6`=二聲、`3`=三聲、`4`=四聲、`7`=輕聲）是音節的分隔點；一聲無符號。輸入先轉小寫（處理大寫亂碼如 `JI394T`）。無法對應的字元會先收掉手上的音節、再略過該字元。

**Files:**
- Create: `src/engine/layout.js`
- Test: `test/layout.test.js`

- [ ] **Step 1: 寫失敗測試**

Create `test/layout.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { keysToZhuyin } from '../src/engine/layout.js';

test('單一音節含三聲：ji3 → ㄨㄛˇ', () => {
  assert.deepEqual(keysToZhuyin('ji3'), ['ㄨㄛˇ']);
});

test('四聲：94 → ㄞˋ', () => {
  assert.deepEqual(keysToZhuyin('94'), ['ㄞˋ']);
});

test('一聲用空白鍵，且空白為分隔點：t → 空白 → ㄔ', () => {
  assert.deepEqual(keysToZhuyin('t '), ['ㄔ']);
});

test('大寫輸入正常還原：JI394T AU04 → 我愛吃麵的注音', () => {
  assert.deepEqual(
    keysToZhuyin('JI394T AU04'),
    ['ㄨㄛˇ', 'ㄞˋ', 'ㄔ', 'ㄇㄧㄢˋ']
  );
});

test('第二聲：6 → ˊ（ej/6 → ㄍㄨㄥˊ）', () => {
  assert.deepEqual(keysToZhuyin('ej/6'), ['ㄍㄨㄥˊ']);
});

test('輕聲：7（j7 → ㄨ˙）', () => {
  assert.deepEqual(keysToZhuyin('j7'), ['ㄨ˙']);
});

test('結尾無聲調鍵時，手上的音節視為一聲收掉', () => {
  assert.deepEqual(keysToZhuyin('ji'), ['ㄨㄛ']);
});

test('無法對應的字元會先收音節再略過', () => {
  assert.deepEqual(keysToZhuyin('ji3@94'), ['ㄨㄛˇ', 'ㄞˋ']);
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test test/layout.test.js`
Expected: FAIL，錯誤訊息類似 `Cannot find module '../src/engine/layout.js'`。

- [ ] **Step 3: 實作 layout.js**

Create `src/engine/layout.js`:

```js
// 大千（標準）注音鍵盤佈局：英文鍵 → 注音符號
export const LAYOUT = {
  '1': 'ㄅ', 'q': 'ㄆ', 'a': 'ㄇ', 'z': 'ㄈ',
  '2': 'ㄉ', 'w': 'ㄊ', 's': 'ㄋ', 'x': 'ㄌ',
  'e': 'ㄍ', 'd': 'ㄎ', 'c': 'ㄏ',
  'r': 'ㄐ', 'f': 'ㄑ', 'v': 'ㄒ',
  '5': 'ㄓ', 't': 'ㄔ', 'g': 'ㄕ', 'b': 'ㄖ',
  'y': 'ㄗ', 'h': 'ㄘ', 'n': 'ㄙ',
  'u': 'ㄧ', 'j': 'ㄨ', 'm': 'ㄩ',
  '8': 'ㄚ', 'i': 'ㄛ', 'k': 'ㄜ', ',': 'ㄝ',
  '9': 'ㄞ', 'o': 'ㄟ', 'l': 'ㄠ', '.': 'ㄡ',
  '0': 'ㄢ', 'p': 'ㄣ', ';': 'ㄤ', '/': 'ㄥ',
  '-': 'ㄦ',
};

// 聲調鍵 → 聲調號（1=一聲 … 5=輕聲）；一聲用空白鍵
export const TONE_KEYS = { ' ': 1, '6': 2, '3': 3, '4': 4, '7': 5 };

// 聲調號 → 注音聲調符號（一聲無符號）
export const TONE_MARKS = { 1: '', 2: 'ˊ', 3: 'ˇ', 4: 'ˋ', 5: '˙' };

// 把英文鍵位字串還原成注音音節陣列（每個音節含聲調符號）
export function keysToZhuyin(input) {
  const syllables = [];
  let current = '';
  for (const ch of input.toLowerCase()) {
    if (Object.hasOwn(TONE_KEYS, ch)) {
      if (current) {
        syllables.push(current + TONE_MARKS[TONE_KEYS[ch]]);
        current = '';
      }
      continue;
    }
    if (Object.hasOwn(LAYOUT, ch)) {
      current += LAYOUT[ch];
      continue;
    }
    // 無法對應的字元：先收掉手上的音節（視為一聲），再略過此字元
    if (current) {
      syllables.push(current);
      current = '';
    }
  }
  if (current) syllables.push(current);
  return syllables;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test test/layout.test.js`
Expected: PASS，8 個測試全過。

- [ ] **Step 5: Commit**

```bash
git add src/engine/layout.js test/layout.test.js
git commit -m "feat: 大千鍵位→注音還原（layout.js）"
```

---

### Task 3: 注音 → 漢字（convert.js）

以「最長詞優先」選字：從左到右，每個位置嘗試最長 4 個音節能否成詞，成詞就取詞頻最高的詞，否則退回單音節。詞庫以參數 `dict`（`Map`，key = 注音音節串接、value = `[[詞, 詞頻], ...]` 依詞頻降冪）注入。無對應時保留注音當 fallback。

**Files:**
- Create: `src/engine/convert.js`
- Test: `test/convert.test.js`

- [ ] **Step 1: 寫失敗測試（含 fixture 詞庫）**

Create `test/convert.test.js`:

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { convert } from '../src/engine/convert.js';

// fixture 詞庫：value 依詞頻降冪；含單字與詞
const dict = new Map([
  ['ㄨㄛˇ', [['我', 1000]]],
  ['ㄞˋ', [['愛', 500], ['礙', 30]]],
  ['ㄔ', [['吃', 300], ['尺', 80], ['池', 60]]],
  ['ㄇㄧㄢˋ', [['麵', 500], ['面', 400]]],
  ['ㄞˋㄔ', [['愛吃', 150]]],
  ['ㄒㄧㄢˋ', [['現', 500], ['線', 300]]],
  ['ㄕㄤˋ', [['上', 900]]],
  ['ㄍㄨㄥ', [['工', 400], ['公', 300]]],
  ['ㄐㄩˋ', [['具', 200], ['句', 150]]],
  ['ㄨㄤˇ', [['網', 250], ['往', 200]]],
  ['ㄒㄧㄢˋㄕㄤˋ', [['線上', 180]]],
  ['ㄍㄨㄥㄐㄩˋ', [['工具', 160]]],
]);

test('單字：ji3 → 我', () => {
  assert.equal(convert('ji3', dict), '我');
});

test('最長詞優先：愛吃 而非 礙尺（94t → 愛吃）', () => {
  assert.equal(convert('94t ', dict), '愛吃');
});

test('無對應音節時保留注音當 fallback', () => {
  // ㄅ（key "1"）+ 空白，詞庫中無此音節
  assert.equal(convert('1 ', dict), 'ㄅ');
});

test('golden：ji394t au04 → 我愛吃麵', () => {
  assert.equal(convert('ji394t au04', dict), '我愛吃麵');
});

test('golden：vu04g;4ej/ rm4j;3 → 線上工具網', () => {
  assert.equal(convert('vu04g;4ej/ rm4j;3', dict), '線上工具網');
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test test/convert.test.js`
Expected: FAIL，錯誤訊息類似 `Cannot find module '../src/engine/convert.js'`。

- [ ] **Step 3: 實作 convert.js**

Create `src/engine/convert.js`:

```js
import { keysToZhuyin } from './layout.js';

const MAX_WORD_LEN = 4;

// input：英文鍵位亂碼字串
// dict：Map，key = 注音音節串接，value = [[詞, 詞頻], ...]（依詞頻降冪）
// 回傳：最可能的漢字串
export function convert(input, dict) {
  const syllables = keysToZhuyin(input);
  let out = '';
  let i = 0;
  while (i < syllables.length) {
    let matched = false;
    const maxLen = Math.min(MAX_WORD_LEN, syllables.length - i);
    for (let len = maxLen; len >= 1; len--) {
      const key = syllables.slice(i, i + len).join('');
      const entry = dict.get(key);
      if (entry && entry.length) {
        out += entry[0][0];
        i += len;
        matched = true;
        break;
      }
    }
    if (!matched) {
      out += syllables[i];
      i += 1;
    }
  }
  return out;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test test/convert.test.js`
Expected: PASS，5 個測試全過（含兩個 golden 範例）。

- [ ] **Step 5: Commit**

```bash
git add src/engine/convert.js test/convert.test.js
git commit -m "feat: 最長詞優先注音選字（convert.js）"
```

---

### Task 4: 全套測試回歸確認

**Files:**
- 無新增；執行整套測試。

- [ ] **Step 1: 執行全部測試**

Run: `npm test`
Expected: 全數通過（smoke 1 + layout 8 + convert 5 = 14 個測試 pass，0 fail）。

- [ ] **Step 2: 更新進度並提交**

編輯 `progress.json`：把 `engine`（含 `layout`、`convert` 子項）的 `status` 設為 `completed`、`progress` 設為 100；`dict` 子項因僅用 fixture、真實詞庫待計畫 2，設為 `in_progress`、`progress` 40。

```bash
git add progress.json
git commit -m "chore: 計畫1核心引擎完成，更新進度"
```

---

## 計畫 1 完成後

引擎可獨立以 `convert(亂碼, dict)` 運作並通過 golden 範例。接著進入：

- **計畫 2**：查證並接上授權清楚的開源注音詞庫（如 libchewing 系）、以 `build-dict.js` 前處理、`convert` 擴充為回傳前 3 名候選、實作 `detect.js` 三道關卡。
- **計畫 3**：Chrome 擴充外殼（`content.js` 監看/替換、游標定位）、Shadow DOM 候選窗、設定頁、esbuild 打包上架。

---

## Self-Review

- **Spec 覆蓋**：本計畫對應 spec 的「①鍵位→注音」「②注音→漢字（最長詞優先）」「模組拆分之 layout.js／convert.js」「測試（TDD、golden 範例）」。詞庫來源、多候選、detect.js、擴充外殼、UI、打包皆明列於計畫 2／3。
- **Placeholder 掃描**：無 TBD／TODO；每個 code step 都有完整程式碼與可執行指令。
- **型別一致**：`keysToZhuyin(input) → string[]`、`convert(input, dict) → string`、`dict` 為 `Map<string, Array<[string, number]>>`，跨 task 一致。
