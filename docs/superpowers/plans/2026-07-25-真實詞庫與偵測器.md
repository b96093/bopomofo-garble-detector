# 真實詞庫 + 多候選 + 偵測器 Implementation Plan（計畫 2／3）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 接上真實開源注音詞庫、讓引擎回傳前 3 名候選、並做出「這串到底是不是注音亂碼」的三道關卡偵測器。

**Architecture:** 開發期 `build-dict.js` 把小麥注音的原始資料（`BPMFMappings.txt` 詞→注音、`phrase.occ` 詞頻）反轉並排序成精簡字典，輸出成可被 Node 與瀏覽器共用的資料模組。`dict.js` 載入成 `Map`。`convert.js` 改用「空格分隔」的注音 key（避免 `ㄧ`+`ㄢ` 與 `ㄧㄢ` 碰撞），並新增 `convertCandidates` 回傳多候選。`detect.js` 用三道關卡（結構→詞庫→英文）判斷是否為亂碼。全程 `node --test`、零第三方相依。

**Tech Stack:** Node.js 內建測試、ES Modules、Node `fs`（僅 build 腳本）、無 npm 相依。

**資料授權：** 小麥注音（openvanilla/McBopomofo）MIT；多字詞源自 libtabe `tsi.src`（BSD）。散布時於擴充內附出處聲明即可。

---

## File Structure

- `build/data/`（**gitignore**）— 放下載的原始資料 `BPMFMappings.txt`、`phrase.occ`、`BPMFBase.txt`。
- `build/build-dict.js` — 開發期腳本：讀 `build/data/`，輸出 `src/engine/dict-data.js`。
- `src/engine/dict-data.js`（build 產物，**committed**）— `export const DICT_TEXT = "<精簡字典壓縮字串>"`。
- `src/engine/dict.js` — `loadDict()`：把 `DICT_TEXT` 解析成 `Map<注音key, [[詞,詞頻],...]>`。
- `src/engine/convert.js`（**修改**）— key 改空格分隔；新增 `convertCandidates(input, dict, n)`。
- `src/engine/detect.js` — `detect(input, dict, opts)`：三道關卡。
- `src/engine/english-common.js` — 少量常見英文字集合（Gate 3 保險）。
- `docs/NOTICE.md` — 資料來源與授權聲明。
- 測試：`test/build-dict.test.js`、`test/convert-candidates.test.js`、`test/detect.test.js`、`test/dict-integration.test.js`。

---

### Task 1: 下載原始資料 + 授權聲明 + gitignore

**Files:**
- Create: `build/data/BPMFMappings.txt`, `build/data/phrase.occ`, `build/data/BPMFBase.txt`（下載）
- Modify: `.gitignore`
- Create: `docs/NOTICE.md`

- [ ] **Step 1: 建立資料夾並下載三個來源檔**

Run（Git Bash）:
```bash
mkdir -p build/data
BASE=https://raw.githubusercontent.com/openvanilla/McBopomofo/master/Source/Data
curl -s "$BASE/BPMFMappings.txt" -o build/data/BPMFMappings.txt
curl -s "$BASE/phrase.occ"       -o build/data/phrase.occ
curl -s "$BASE/BPMFBase.txt"     -o build/data/BPMFBase.txt
wc -l build/data/*.txt build/data/*.occ
```
Expected: 三檔皆下載成功、行數各數萬到數十萬（BPMFMappings 最多）。

- [ ] **Step 2: 記錄實際格式（讀前 5 行確認）**

Run:
```bash
head -n 5 build/data/BPMFMappings.txt
head -n 5 build/data/BPMFBase.txt
head -n 5 build/data/phrase.occ
```
確認並在心中對照（後續 build 腳本依此解析）：
- `BPMFMappings.txt`：`詞 注音1 注音2 …`（空白分隔；一聲無符號）。
- `BPMFBase.txt`：`單字 注音 拼音 碼 編碼`（取前兩欄：字、注音）。
- `phrase.occ`：`詞 次數`。

若實際格式與上述不符（欄位順序不同等），**STOP 並回報 BLOCKED**，附上實際前 5 行，讓控制者調整 Task 2 的解析邏輯。

- [ ] **Step 3: 把原始資料加入 .gitignore（不進版庫）**

在 `.gitignore` 追加一行：
```
build/data/
```

- [ ] **Step 4: 寫授權聲明 `docs/NOTICE.md`**

Create `docs/NOTICE.md`:
```markdown
# 第三方資料授權聲明

本工具的注音詞庫由下列開源資料前處理而成：

- **McBopomofo 小麥注音**（https://github.com/openvanilla/McBopomofo）— MIT License。
- 多字詞資料源自 **libtabe** 的 `tsi.src`（BSD License），經 McBopomofo 整理。

依 MIT / BSD 授權，散布本工具時保留此聲明即符合規定。
```

- [ ] **Step 5: Commit**（只進 NOTICE 與 .gitignore；原始資料被忽略）
```bash
git add .gitignore docs/NOTICE.md
git commit -m "chore: 詞庫來源授權聲明與原始資料 gitignore"
```

---

### Task 2: build-dict.js（TDD，小樣本）

先用小樣本 fixture 驗證解析與反轉邏輯，再於 Task 3 跑真實資料。

**Files:**
- Create: `build/build-dict.js`
- Test: `test/build-dict.test.js`
- Test fixtures: `test/fixtures/mappings.sample.txt`, `test/fixtures/occ.sample.txt`, `test/fixtures/base.sample.txt`

- [ ] **Step 1: 建立樣本 fixture**

Create `test/fixtures/mappings.sample.txt`:
```
我 ㄨㄛˇ
愛 ㄞˋ
愛吃 ㄞˋ ㄔ
吃 ㄔ
麵 ㄇㄧㄢˋ
面 ㄇㄧㄢˋ
```

Create `test/fixtures/base.sample.txt`:
```
我 ㄨㄛˇ wo3 -3 utf8
愛 ㄞˋ ai4 -4 utf8
```

Create `test/fixtures/occ.sample.txt`:
```
我 1000
愛 500
愛吃 150
吃 300
麵 500
面 400
```

- [ ] **Step 2: 寫失敗測試**

Create `test/build-dict.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { buildDict, serializeDict } from '../build/build-dict.js';

const dir = new URL('./fixtures/', import.meta.url);
const mappings = new URL('mappings.sample.txt', dir);
const base = new URL('base.sample.txt', dir);
const occ = new URL('occ.sample.txt', dir);

test('反轉：注音 key（空格分隔）→ 依詞頻降冪的候選', () => {
  const map = buildDict({ mappings, base, occ });
  // 單音節
  assert.deepEqual(map.get('ㄨㄛˇ'), [['我', 1000]]);
  // 同音字依詞頻降冪：麵(500) 在 面(400) 前
  assert.deepEqual(map.get('ㄇㄧㄢˋ'), [['麵', 500], ['面', 400]]);
  // 多音節詞，key 用空格分隔
  assert.deepEqual(map.get('ㄞˋ ㄔ'), [['愛吃', 150]]);
});

test('serializeDict 產出可再解析的緊湊字串', () => {
  const map = buildDict({ mappings, base, occ });
  const text = serializeDict(map);
  // 每行：key\t詞1 頻1 詞2 頻2 ...
  const lines = text.split('\n').filter(Boolean);
  const mianLine = lines.find((l) => l.startsWith('ㄇㄧㄢˋ\t'));
  assert.equal(mianLine, 'ㄇㄧㄢˋ\t麵 500 面 400');
});
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `node --test test/build-dict.test.js`
Expected: FAIL（`Cannot find module '../build/build-dict.js'`）。

- [ ] **Step 4: 實作 build-dict.js**

Create `build/build-dict.js`:
```js
import { readFileSync, writeFileSync } from 'node:fs';

const MAX_CANDIDATES_PER_KEY = 10;

function readLines(url) {
  return readFileSync(url, 'utf8').split('\n').map((l) => l.trim()).filter(Boolean);
}

// 回傳 Map<注音key（空格分隔）, [[詞, 詞頻], ...]（詞頻降冪）>
export function buildDict({ mappings, base, occ }) {
  // 詞頻表
  const freq = new Map();
  for (const line of readLines(occ)) {
    const sp = line.lastIndexOf(' ');
    const word = line.slice(0, sp);
    const count = Number(line.slice(sp + 1));
    if (word && Number.isFinite(count)) freq.set(word, count);
  }

  // key -> Map<詞, 詞頻>（暫存去重）
  const acc = new Map();
  const add = (key, word) => {
    if (!key || !word) return;
    if (!acc.has(key)) acc.set(key, new Map());
    const inner = acc.get(key);
    const f = freq.get(word) ?? 0;
    if (!inner.has(word) || inner.get(word) < f) inner.set(word, f);
  };

  // 多字詞：詞 + 注音音節（空格分隔）
  for (const line of readLines(mappings)) {
    const parts = line.split(/\s+/);
    const word = parts[0];
    const key = parts.slice(1).join(' ');
    add(key, word);
  }

  // 單字：字 + 注音（取前兩欄）
  for (const line of readLines(base)) {
    const parts = line.split(/\s+/);
    add(parts[1], parts[0]);
  }

  // 排序 + 截斷
  const out = new Map();
  for (const [key, inner] of acc) {
    const entries = [...inner.entries()]
      .sort((a, b) => b[1] - a[1])
      .slice(0, MAX_CANDIDATES_PER_KEY);
    out.set(key, entries);
  }
  return out;
}

// Map -> 緊湊字串：每行 `key\t詞1 頻1 詞2 頻2 ...`
export function serializeDict(map) {
  const lines = [];
  for (const [key, entries] of map) {
    const flat = entries.map(([w, f]) => `${w} ${f}`).join(' ');
    lines.push(`${key}\t${flat}`);
  }
  return lines.join('\n');
}

// CLI：node build/build-dict.js  → 產生 src/engine/dict-data.js
function main() {
  const dataDir = new URL('./data/', import.meta.url);
  const map = buildDict({
    mappings: new URL('BPMFMappings.txt', dataDir),
    base: new URL('BPMFBase.txt', dataDir),
    occ: new URL('phrase.occ', dataDir),
  });
  const text = serializeDict(map);
  const outUrl = new URL('../src/engine/dict-data.js', import.meta.url);
  writeFileSync(outUrl, `export const DICT_TEXT = ${JSON.stringify(text)};\n`, 'utf8');
  console.log(`dict entries: ${map.size}, bytes: ${text.length}`);
}

if (import.meta.url === `file://${process.argv[1]}` || process.argv[1]?.endsWith('build-dict.js')) {
  main();
}
```

- [ ] **Step 5: 執行測試確認通過**

Run: `node --test test/build-dict.test.js`
Expected: PASS，2 個測試全過。

- [ ] **Step 6: Commit**
```bash
git add build/build-dict.js test/build-dict.test.js test/fixtures/
git commit -m "feat: build-dict 反轉詞庫為注音→詞（TDD 小樣本）"
```

---

### Task 3: 產生真實字典 + convert 改空格 key + 整合測試

**Files:**
- Create: `src/engine/dict-data.js`（build 產物，committed）
- Create: `src/engine/dict.js`
- Modify: `src/engine/convert.js`（key `.join('')` → `.join(' ')`）
- Modify: `test/convert.test.js`（fixture key 改空格分隔）
- Test: `test/dict-integration.test.js`

- [ ] **Step 1: 跑 build 產生真實字典**

Run:
```bash
node build/build-dict.js
ls -la src/engine/dict-data.js
```
Expected: 印出 `dict entries: <數萬>, bytes: <數百萬>`，`src/engine/dict-data.js` 產生。

- [ ] **Step 2: 實作 dict.js（載入器）**

Create `src/engine/dict.js`:
```js
import { DICT_TEXT } from './dict-data.js';

// 解析緊湊字串為 Map<注音key, [[詞, 詞頻], ...]>
export function loadDict() {
  const map = new Map();
  for (const line of DICT_TEXT.split('\n')) {
    if (!line) continue;
    const tab = line.indexOf('\t');
    if (tab < 0) continue;
    const key = line.slice(0, tab);
    const toks = line.slice(tab + 1).split(' ');
    const entries = [];
    for (let i = 0; i + 1 < toks.length; i += 2) {
      entries.push([toks[i], Number(toks[i + 1])]);
    }
    map.set(key, entries);
  }
  return map;
}
```

- [ ] **Step 3: 修改 convert.js 使用空格分隔 key**

在 `src/engine/convert.js` 中，把組 key 的那行
```js
const key = syllables.slice(i, i + len).join('');
```
改為
```js
const key = syllables.slice(i, i + len).join(' ');
```
（其餘不變。）

- [ ] **Step 4: 同步修改 convert.test.js 的 fixture key（改空格分隔）**

在 `test/convert.test.js` 的 fixture `dict` 中，把多音節 key 加空格：
- `'ㄞˋㄔ'` → `'ㄞˋ ㄔ'`
- `'ㄒㄧㄢˋㄕㄤˋ'` → `'ㄒㄧㄢˋ ㄕㄤˋ'`
- `'ㄍㄨㄥㄐㄩˋ'` → `'ㄍㄨㄥ ㄐㄩˋ'`
（單音節 key 不變。）

- [ ] **Step 5: 寫真實字典整合測試**

Create `test/dict-integration.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { loadDict } from '../src/engine/dict.js';
import { convert } from '../src/engine/convert.js';

const dict = loadDict();

test('真實字典非空', () => {
  assert.ok(dict.size > 1000);
});

test('golden：ji394t au04 → 我愛吃麵', () => {
  assert.equal(convert('ji394t au04', dict), '我愛吃麵');
});

test('golden：vu04g;4ej/ rm4j;3 → 線上工具網', () => {
  assert.equal(convert('vu04g;4ej/ rm4j;3', dict), '線上工具網');
});
```

- [ ] **Step 6: 執行 convert 與整合測試**

Run: `node --test test/convert.test.js test/dict-integration.test.js`
Expected: convert 5/5 過；整合 3/3 過。若 golden 未過（真實字典的同音字排序或斷詞不同），**STOP 回報 DONE_WITH_CONCERNS**，附上實際輸出，讓控制者判斷是否調整（可能需在整合測試放寬為「包含關鍵詞」或補詞頻微調）。

- [ ] **Step 7: Commit**
```bash
git add src/engine/dict-data.js src/engine/dict.js src/engine/convert.js test/convert.test.js test/dict-integration.test.js
git commit -m "feat: 接上真實注音詞庫，convert 改空格分隔 key"
```

---

### Task 4: convert 多候選（前 3 名）

**Files:**
- Modify: `src/engine/convert.js`（新增 `convertCandidates`）
- Test: `test/convert-candidates.test.js`

- [ ] **Step 1: 寫失敗測試**

Create `test/convert-candidates.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { convertCandidates } from '../src/engine/convert.js';

const dict = new Map([
  ['ㄨㄛˇ', [['我', 1000]]],
  ['ㄞˋ', [['愛', 500], ['礙', 30]]],
  ['ㄔ', [['吃', 300], ['尺', 80]]],
  ['ㄇㄧㄢˋ', [['麵', 500], ['面', 400]]],
  ['ㄞˋ ㄔ', [['愛吃', 150]]],
]);

test('第一候選為貪婪最佳解', () => {
  const c = convertCandidates('ji394t au04', dict, 3);
  assert.equal(c[0], '我愛吃麵');
});

test('回傳至多 n 個、且彼此不同', () => {
  const c = convertCandidates('ji394t au04', dict, 3);
  assert.ok(c.length >= 2 && c.length <= 3);
  assert.equal(new Set(c).size, c.length);
  // 麵/面 差距最小，應出現「我愛吃面」為替代
  assert.ok(c.includes('我愛吃面'));
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `node --test test/convert-candidates.test.js`
Expected: FAIL（`convertCandidates is not a function`）。

- [ ] **Step 3: 實作 convertCandidates（加到 convert.js）**

在 `src/engine/convert.js` 末端新增（沿用檔案既有的 `keysToZhuyin` import 與 `MAX_WORD_LEN`）：
```js
// 貪婪斷詞，回傳每段的候選清單
function segment(input, dict) {
  const syllables = keysToZhuyin(input);
  const segments = [];
  let i = 0;
  while (i < syllables.length) {
    let matched = false;
    const maxLen = Math.min(MAX_WORD_LEN, syllables.length - i);
    for (let len = maxLen; len >= 1; len--) {
      const key = syllables.slice(i, i + len).join(' ');
      const entry = dict.get(key);
      if (entry && entry.length) {
        segments.push(entry);
        i += len;
        matched = true;
        break;
      }
    }
    if (!matched) {
      segments.push([[syllables[i], 0]]);
      i += 1;
    }
  }
  return segments;
}

// 回傳至多 n 個完整候選字串，最佳在前。
// 替代解：對每個「有次佳詞」的段落做單點替換，依詞頻差距（小者優先）排序。
export function convertCandidates(input, dict, n = 3) {
  const segments = segment(input, dict);
  const best = segments.map((s) => s[0][0]).join('');

  const swaps = [];
  for (let idx = 0; idx < segments.length; idx++) {
    if (segments[idx].length >= 2) {
      const gap = segments[idx][0][1] - segments[idx][1][1];
      const variant = segments
        .map((s, j) => (j === idx ? s[1][0] : s[0][0]))
        .join('');
      swaps.push([gap, variant]);
    }
  }
  swaps.sort((a, b) => a[0] - b[0]);

  const result = [best];
  for (const [, variant] of swaps) {
    if (result.length >= n) break;
    if (!result.includes(variant)) result.push(variant);
  }
  return result;
}
```

- [ ] **Step 4: 執行測試確認通過**

Run: `node --test test/convert-candidates.test.js`
Expected: PASS，2 個測試全過。

- [ ] **Step 5: Commit**
```bash
git add src/engine/convert.js test/convert-candidates.test.js
git commit -m "feat: convertCandidates 回傳前 3 名候選"
```

---

### Task 5: detect.js 三道關卡偵測器

**Files:**
- Create: `src/engine/english-common.js`
- Create: `src/engine/detect.js`
- Test: `test/detect.test.js`

- [ ] **Step 1: 建立常見英文字集合**

Create `src/engine/english-common.js`:
```js
// Gate 3 保險：少量最常見英文字，避免把真英文誤判為亂碼。
export const COMMON_ENGLISH = new Set([
  'the', 'be', 'to', 'of', 'and', 'a', 'in', 'that', 'have', 'it',
  'for', 'not', 'on', 'with', 'he', 'as', 'you', 'do', 'at', 'this',
  'but', 'his', 'by', 'from', 'they', 'we', 'say', 'her', 'she', 'or',
  'an', 'will', 'my', 'one', 'all', 'would', 'there', 'their', 'what',
  'so', 'up', 'out', 'if', 'about', 'who', 'get', 'which', 'go', 'me',
  'when', 'make', 'can', 'like', 'time', 'no', 'just', 'him', 'know',
  'take', 'people', 'into', 'year', 'your', 'good', 'some', 'could',
  'them', 'see', 'other', 'than', 'then', 'now', 'look', 'only', 'come',
  'its', 'over', 'think', 'also', 'hello', 'hi', 'yes', 'ok', 'okay',
]);
```

- [ ] **Step 2: 寫失敗測試**

Create `test/detect.test.js`:
```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { loadDict } from '../src/engine/dict.js';
import { detect } from '../src/engine/detect.js';

const dict = loadDict();

test('注音亂碼：回傳候選', () => {
  const r = detect('ji394t au04', dict);
  assert.ok(r);
  assert.equal(r.candidates[0], '我愛吃麵');
});

test('真英文句子：不觸發（回傳 null）', () => {
  assert.equal(detect('the cat sat', dict), null);
});

test('常見英文單字：不觸發', () => {
  assert.equal(detect('hello', dict), null);
});

test('空字串：不觸發', () => {
  assert.equal(detect('', dict), null);
});
```

- [ ] **Step 3: 執行測試確認失敗**

Run: `node --test test/detect.test.js`
Expected: FAIL（`Cannot find module '../src/engine/detect.js'`）。

- [ ] **Step 4: 實作 detect.js**

Create `src/engine/detect.js`:
```js
import { keysToZhuyin } from './layout.js';
import { convertCandidates } from './convert.js';
import { COMMON_ENGLISH } from './english-common.js';

const HAN = /[一-鿿㐀-䶿]/;
// 亂碼只由：字母、聲調數字 3/4/6/7、空白、及大千標點鍵組成
const KEYSTROKE = /^[a-z0-9 ;/.,-]+$/i;

function hanRatio(s) {
  if (!s) return 0;
  let n = 0;
  for (const ch of s) if (HAN.test(ch)) n++;
  return n / [...s].length;
}

// 回傳 { candidates:string[], confidence:number } 或 null（非亂碼）
export function detect(input, dict, opts = {}) {
  const threshold = opts.threshold ?? 0.8; // 保守；設定頁「積極」可調低
  const minSyllables = opts.minSyllables ?? 2;

  const text = input;
  if (!text || !KEYSTROKE.test(text)) return null;

  // Gate 3：整串就是常見英文字 → 不理
  if (COMMON_ENGLISH.has(text.trim().toLowerCase())) return null;

  // Gate 1（結構）：每個音節都是合法（單音節在字典裡查得到）
  const syllables = keysToZhuyin(text);
  if (syllables.length < minSyllables) return null;
  if (!syllables.every((s) => dict.has(s))) return null;

  // Gate 2（詞庫）：轉出的最佳解幾乎全是漢字
  const candidates = convertCandidates(text, dict, 3);
  const ratio = hanRatio(candidates[0]);
  if (ratio < threshold) return null;

  return { candidates, confidence: ratio };
}
```

- [ ] **Step 5: 執行測試確認通過**

Run: `node --test test/detect.test.js`
Expected: PASS，4 個測試全過。若「注音亂碼」案例未回傳候選（因真實字典某音節查不到），**STOP 回報 DONE_WITH_CONCERNS** 附實際輸出，讓控制者決定是否放寬 Gate 1（例如改用「合法注音音節正則」而非 `dict.has`）。

- [ ] **Step 6: Commit**
```bash
git add src/engine/english-common.js src/engine/detect.js test/detect.test.js
git commit -m "feat: detect 三道關卡亂碼偵測器（結構/詞庫/英文）"
```

---

### Task 6: 全套回歸 + 更新進度

- [ ] **Step 1: 執行全部測試**

Run: `npm test`
Expected: 全數通過（Plan 1 的 17 + build-dict 2 + 整合 3 + candidates 2 + detect 4 = 28）。

- [ ] **Step 2: 更新 `progress.json`**

把 `engine` 設 `completed`/100、其 `dict` 子項設 `completed`/100；`detect` 部分設 `completed`/100。

```bash
git add progress.json
git commit -m "chore: 計畫2完成（真實詞庫+多候選+偵測器），更新進度"
```

---

## 計畫 2 完成後

引擎具備：真實詞庫、前 3 候選、保守三道關卡偵測。接著 **計畫 3**：Chrome 擴充外殼（`content.js` 監看輸入框/游標定位/替換）、Shadow DOM 候選窗、設定頁（保守/積極 threshold）、esbuild 打包上架。

---

## Self-Review

- **Spec 覆蓋**：對應 spec「②注音→漢字（詞庫）」升級為真實詞庫、「偵測器三道關卡」、「convert 多候選（候選窗用）」、「資料與授權」。擴充外殼/UI/設定頁/打包在計畫 3。
- **Placeholder 掃描**：無 TBD；每個 code step 均含完整程式碼與可執行指令。外部資料格式在 Task 1 Step 2 明確驗證，格式不符時有 BLOCKED 出口。
- **型別一致**：`buildDict({mappings,base,occ}) → Map<string,[string,number][]>`；`serializeDict(Map) → string`；`loadDict() → Map`；`convert(input,dict) → string`；`convertCandidates(input,dict,n) → string[]`；`detect(input,dict,opts) → {candidates:string[],confidence:number}|null`。字典 key 一律「注音音節以單一空格分隔」，build/convert/detect 三處一致。
- **已知風險出口**：真實字典的同音字排序可能與 fixture 不同，Task 3/5 皆有 DONE_WITH_CONCERNS/BLOCKED 出口讓控制者調整，不會靜默硬過。
